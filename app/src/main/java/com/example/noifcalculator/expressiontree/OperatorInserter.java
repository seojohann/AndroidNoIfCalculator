package com.example.noifcalculator.expressiontree;

/**
 * Use this interface when inserting to a tree. used to distinguish which operator it's
 * inserting
 */
public interface OperatorInserter {
    void insertOperator();
}
